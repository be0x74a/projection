# Patterns to ignore when building packages.
.DS_Store
.git/
.gitignore
.vscode/
.idea/
*.swp
*.bak
*.tmp
*.tgz
*.orig
node_modules/
# Pushed as a separate OCI artifact (tag :artifacthub.io) by the release
# workflow; not installable chart content.
artifacthub-repo.yml
