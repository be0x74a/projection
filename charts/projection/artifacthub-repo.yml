# Artifact Hub repository metadata.
#
# This file is published as a separate OCI artifact (tag :artifacthub.io) on
# the same registry namespace as the Helm chart by
# .github/workflows/helm-release.yml. Artifact Hub fetches it to verify
# ownership of the OCI repository and grant verified-publisher status.
repositoryID: ff31aa87-81b2-46d4-9412-63a9373866a1
owners:
  - name: projection maintainers
    email: maintainers@projection.sh
