// Package e2e_upgrade hosts the v0.1 -> v0.2 upgrade-path e2e. Separate from
// test/e2e so it can own the operator install lifecycle instead of relying on
// a pre-installed operator.
package e2e_upgrade
