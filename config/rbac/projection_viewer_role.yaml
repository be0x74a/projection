# permissions for end users to view projections.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: projection
    app.kubernetes.io/managed-by: kustomize
  name: projection-viewer-role
rules:
- apiGroups:
  - projection.be0x74a.io
  resources:
  - projections
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - projection.be0x74a.io
  resources:
  - projections/status
  verbs:
  - get
