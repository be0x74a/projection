version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - dupl
    - errcheck
    - ginkgolinter
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
  settings:
    revive:
      rules:
        - name: comment-spacings
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
          - staticcheck
        path: api/*
      - linters:
          - dupl
          - goconst
          - lll
        path: internal/*
      - linters:
          - dupl
          - goconst
          - lll
        path: test/bench/*
      - linters:
          - staticcheck
        path: test/utils/*
      - linters:
          - dupl
          - goconst
          - gocyclo
          - lll
          - unparam
        path: _test\.go$
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
